Vim Envionment
=============

Installation
-----------

	$ git clone git://github.com/winfred-lu/dotvim.git ~/.vim
	$ ln -sf ~/.vim/vimrc ~/.vimrc
	$ cd ~/.vim
	$ git submodule init
	$ git submodule update

Usage
-----------

	$ vim
